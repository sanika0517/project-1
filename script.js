
function performSearch() {
    var category = document.querySelector('.search-dropdown').value;
    var query = document.querySelector('.search-box').value;
    alert('Category: ' + category + '\nSearch Query: ' + query);
    // You can implement your search logic here, e.g., redirecting to a search results page
}